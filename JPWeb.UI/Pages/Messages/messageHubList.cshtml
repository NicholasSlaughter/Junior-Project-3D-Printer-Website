@page
@model JPWeb.UI.Pages.Messages.messageHubListModel
@{
    ViewData["Title"] = "messageHubList";
}

<br />
<br />
<br />
<h2>Message Hubs</h2>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProjectName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastMessage)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody id="table">
        @foreach (var item in Model._Messages)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Sender.Email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Sender.Requests.LastOrDefault().ProjectName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Sender.LatestMessage)

                    <a asp-page="/Messages/AdminMessagePage" asp-route-id="@item.Sender.Id">Message</a>

                </td>
            </tr>
        }
    </tbody>
</table>